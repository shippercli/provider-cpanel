# Shipper CLI Provider cPanel

Provider package, metadata, and assets for the Shipper cPanel integration.

The implementation targets cPanel account UAPI, the remaining deployment
operations that only exist in cPanel API 2, and optional privileged WHM API
operations. See [the capability model](docs/capabilities.md) for the release
scope and verification gates.
